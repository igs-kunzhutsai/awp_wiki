# raw/ — 原始素材

**唯讀。不維護、不保證正確、不隨 code 更新。**

這裡放「AWP 的 code 改了也不用改」的東西：外部規格書、協定與業界通識、
會議紀錄、從別處匯入的既有文件、我們自己已被取代的舊版。

| | |
|---|---|
| **會怎樣** | 不進 `wiki/index.md` 的導覽，也不列入孤島統計；不推送 Outline |
| **不會怎樣** | 不修改內容、不保證跟 code 對得上、不承諾更新 |

需要現況請看 `modules/` 對應頁；需要原文請回查素材自己標的來源。

## 每頁必須有的標籤與警告

見 `WIKI-SPEC.md` §4 素材區那節。匯入腳本自動加，不靠人記得。

## 子目錄

**預設不分子目錄。** 東西多到不好找再開，開的時候照來源或主題分，
並在本檔加一行說明。

> 本檔是唯一兼作入口的目錄頁 —— 因為 `raw/` 不進 `index.md`，
> 沒有這份就沒有任何地方查得到裡面有什麼（`WIKI-SPEC.md` §2）。
